% Macros mantenidas por MathMongo para documentos y proyectos exportados.
\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{mathmongo-macros}[2026/08/04 Shared MathMongo macros]

% amsmath define \operator. Load it without overriding an existing setup.
\RequirePackage{amsmath}
\providecommand{\osc}{\operatorname{osc}}
\providecommand{\Max}{\operatorname{Max}}

\endinput
