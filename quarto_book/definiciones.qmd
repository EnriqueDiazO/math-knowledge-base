# Definiciones

# Definición: Espacio Métrico

Un espacio métrico es un par $(X, d)$, donde $X$ es un conjunto y $d : X \times X \to \mathbb{R}$ es una función que cumple:

1. $d(x, y) \geq 0$

2. $d(x, y) = 0 \iff x = y$

3. $d(x, y) = d(y, x)$

4. $d(x, z) \leq d(x, y) + d(y, z)$

